export interface signInData {
    apellido: string;
    email: string;
    password: string;
}

export interface logOutData {
    apellido: string;
    token: string;
}

export interface aRequestData {
    token: string;
    password: string;
    email: string;
}
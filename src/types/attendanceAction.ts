export interface newAData {
    students: string[];
    token: string;
}

export interface dSData {
    attendanceId: string;
    studentId: string;
    token: string;
}

export interface dDData {
    attendanceId: string;
    token: string;
}